# 📚 图书馆管理系统 — 使用指南

## 1. MySQL 数据库搭建

打开 MySQL（终端或图形工具如 Navicat），依次执行以下 SQL：

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS library DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE library;

-- 用户表
CREATE TABLE users (
  id          INT AUTO_INCREMENT PRIMARY KEY,
  username    VARCHAR(50)  NOT NULL UNIQUE,
  password    VARCHAR(255) NOT NULL,
  name        VARCHAR(50)  NOT NULL,
  email       VARCHAR(100) DEFAULT NULL,
  phone       VARCHAR(20)  DEFAULT NULL,
  role        ENUM('admin','user') NOT NULL DEFAULT 'user',
  status      TINYINT      NOT NULL DEFAULT 1,
  created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 分类表
CREATE TABLE categories (
  id          INT AUTO_INCREMENT PRIMARY KEY,
  name        VARCHAR(50)  NOT NULL UNIQUE,
  description VARCHAR(255) DEFAULT NULL,
  sort_order  INT          NOT NULL DEFAULT 0,
  created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 图书表
CREATE TABLE books (
  id              INT AUTO_INCREMENT PRIMARY KEY,
  isbn            VARCHAR(20)  NOT NULL UNIQUE,
  title           VARCHAR(200) NOT NULL,
  author          VARCHAR(100) NOT NULL,
  publisher       VARCHAR(100) DEFAULT NULL,
  publish_year    INT          DEFAULT NULL,
  category_id     INT          DEFAULT NULL,
  description     TEXT         DEFAULT NULL,
  cover_url       VARCHAR(500) DEFAULT NULL,
  total_count     INT          NOT NULL DEFAULT 1,
  available_count INT          NOT NULL DEFAULT 1,
  status          TINYINT      NOT NULL DEFAULT 1,
  created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE SET NULL
);

-- 借阅记录表
CREATE TABLE borrow_records (
  id          INT AUTO_INCREMENT PRIMARY KEY,
  user_id     INT NOT NULL,
  book_id     INT NOT NULL,
  borrow_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  due_date    DATE     NOT NULL,
  return_date DATETIME DEFAULT NULL,
  status      ENUM('borrowed','returned','overdue') NOT NULL DEFAULT 'borrowed',
  renew_count INT NOT NULL DEFAULT 0,
  created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (book_id) REFERENCES books(id) ON DELETE CASCADE
);

-- 插入一个初始管理员账号（密码: admin123）
INSERT INTO users (username, password, name, role) VALUES (
  'admin',
  '$2b$10$NAmPW0f6krNm3hn1yuYSu.VTjgvqf7GczvsMds.3S7xtlRNO2ez52',
  '管理员',
  'admin'
);
```

> 初始管理员账号：**admin**，密码：**admin123**。登录后请及时修改密码。

---

## 2. 启动项目

### 后端

```bash
cd backend
npm install
# 编辑 .env 确认数据库连接配置
npm run dev
# -> http://localhost:3000
```

### 前端

```bash
cd frontend
npm install
npm run dev
# -> http://localhost:5173
```

---

## 3. 角色说明

| 角色         | 说明     | 可访问页面                                     |
| ------------ | -------- | ---------------------------------------------- |
| **管理员**   | 全部权限 | 仪表盘、图书管理、借阅管理、用户管理、个人中心 |
| **普通用户** | 个人相关 | 仪表盘、图书列表、我的借阅、个人中心           |

---

## 4. 如何登录

- 浏览器打开 `http://localhost:5173` → 自动跳转到登录页
- 输入**用户名**和**密码** → 点「登录」
- 点顶栏右侧或侧栏底部的「退出登录」可退出

---

## 5. 功能使用

### 📊 仪表盘

查看统计卡片（总图书数/总用户数/当前借出/逾期未还）、借阅趋势折线图、热门图书排行榜、逾期记录列表。

### 📚 图书管理

- **搜索**：按书名/作者/ISBN 搜索，支持分类筛选
- **管理员操作**：新增、编辑、上架/下架图书
- **所有用户**：点击 👁 查看图书详情

### 🗂️ 分类管理

在图书管理页面点「分类管理」打开。可新增、编辑名称/描述、调整排序、删除分类（有图书的分类不可删）。

### 📋 借阅管理（管理员）

- **借书**：点「借书」→ 搜索选择用户 → 搜索选择图书 → 设置应还日期 → 确认
- **还书**：点「还书」→ 确认 → 自动增加可借数量
- **续借**：点「续借」→ 自动延长 14 天
- 可按关键字搜索、按状态（借阅中/已归还/已逾期）筛选

### 📋 我的借阅（普通用户）

查看自己的借阅记录，可自行还书和续借（逾期图书不可续借）。

### 👥 用户管理（管理员）

- 新增/编辑/删除用户
- 启用或禁用用户（禁用后无法登录和借书）
- 重置用户密码

### 👤 个人中心

- 查看自己的信息
- 修改密码（需输入当前密码验证）

### 🌙 主题切换

点顶栏 ☀️ / 🌙 按钮切换深色/浅色主题。

---

## 6. 常见问题

**Q: 登录提示「用户名或密码错误」？**  
A: 检查账号密码；忘记密码请联系管理员重置。

**Q: 看不到「借阅管理」「用户管理」？**  
A: 这些仅管理员可见。普通用户侧栏自动隐藏。

**Q: 借书提示「无可借数量」？**  
A: 该书所有馆藏均已被借出，等待归还后再借。

**Q: 无法借阅某本书？**  
A: 可能原因：书已下架、可借数量为 0、账号被禁用。

---

> 如有其他问题，请联系系统管理员。
